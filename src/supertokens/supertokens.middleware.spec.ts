import { SupertokensMiddleware } from './supertokens.middleware';

describe('SupertokensMiddleware', () => {
  it('should be defined', () => {
    expect(new SupertokensMiddleware()).toBeDefined();
  });
});
